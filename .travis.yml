language: node_js

node_js:
  - "8"

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y chromium-browser

before_script:
  - npm install -g @angular/cli
  - npm install

script:
  - ng test

